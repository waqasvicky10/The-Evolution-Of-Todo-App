# Backend API URL — leave EMPTY to use Next.js proxy (avoids CORS, faster)
# Or set http://localhost:8000 for direct backend (requires CORS on backend)
NEXT_PUBLIC_API_BASE_URL=

# Better Auth Configuration
BETTER_AUTH_SECRET=your-secret-key-at-least-32-characters-long-change-this-in-production
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database URL for Better Auth (should match backend DATABASE_URL)
# For Neon PostgreSQL:
# DATABASE_URL=postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
# For local SQLite:
# DATABASE_URL=file:./auth.db
